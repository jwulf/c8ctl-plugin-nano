node_modules/
.DS_Store
# Release-time staging (generated by scripts/, downloaded from releases)
npm-platforms/
binaries/
# Vendored Effect source (reference-only, populated by `npm run vendor:effect`).
# Kept out of the tree — it is ~51 MB / 2300+ files of read-only reference; the
# pinned checkout is reproducible from scripts/vendor-effect.mjs.
repos/
